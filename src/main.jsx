import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// this should be called `main.tsx`
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);